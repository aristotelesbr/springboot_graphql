type Customer {
  id: ID
  name: String
  email: String
}
extend type Query {
  customer(id: ID): Customer
  customers: [Customer]
}

extend type Mutation {
  saveCustomer(id: ID, name: String, email: String): Customer
}
